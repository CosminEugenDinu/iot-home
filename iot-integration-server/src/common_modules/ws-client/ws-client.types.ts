export enum KWsClient {
  RPI_LOCAL_INTEGRATION_SYSTEM = "rpi-local.integration.system",
  FRONTEND = "FRONTEND",
}

export enum TIdentity {
  SYSTEM_NAME = KWsClient.RPI_LOCAL_INTEGRATION_SYSTEM,
}

export enum KWsHeader {
  WS_CLIENT_NAME = "ws-client-name",
}
